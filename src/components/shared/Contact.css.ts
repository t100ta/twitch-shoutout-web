import { style } from "@vanilla-extract/css";

export const contact = style({
  alignItems: "center",
  justifyContent: "center",
});

export const note = style({
  width: "80vw",
  margin: "5vh 10vw 2.5vh",
});

export const contactLinks = style({});
